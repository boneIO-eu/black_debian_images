# Replaced at the controller's first boot by boneio-mqtt-firstboot with a
# password drawn for this device alone; the broker gets the same one. The
# factory value below only exists so the image's config validates.
mqtt_password: boneio123
